- Exclude unwanted movies (has to be done manually)

LIST TO EXCLUDE:
Shameless,
Kandungan,
Risqué,
Succubus,
Disclosure,
The Voyeur,
Ligaw,
Uhaw,
Scorned,
Rita,
Sayaw,
Malena,
Giacomo Casanova,
Kulong,
Mom's Friend,
My Friend's Mom,
EKS,
Fate/kaleid liner Prisma☆Illya: Dance at the Sports Festival!,
Fate/kaleid liner Prisma☆Illya 2wei!: Magical Girl in Hot Springs Inn,
Les Inshortables, Vol. 1,
Play Time,
High Art,
Emmanuelle

REMOVE:
Movies:
  Hot Girls Wanted
TV Shows:
  Redo of Healer,
  Raw,
  Tagesschau,
  High School D×D


- Make a real README.md with all the steps of functionality (ex: make env files in backend/frontend, run seed_movies.py, build_content_recommender, etc.)/project description
    Setup:
    - Install the dependencies: "pip install -r requirements.txt" from backend + other unlisted dependencies / "npm install package.json" from frontend 
    - Make a .env file in the backend directory with your MONGO_URI and TMDB_KEY
    - Make a .env.local with a VITE_API_BASE=http://localhost:5000
    - Run "python seed_movies.py" from backend
    - Run "python scripts\build_content_recommender.py" from backend (run this every time you rebuild the database with seed_movies.py)
    - To boot up the project, run 'flask run' from backend and 'npm run dev' from frontend

    Features:
    - Search
    - Three different movie sections: Popular, Top, New
    - Recommendations

-DONE- 1. Fix Top Rated (add a cutoff point for the votes ex: min 2000)
-DONE- 2. Make recommendation row start earlier and end later, in order to fit all movies without scrolling

DONE: AUTHENTICATION
--DONE-- - Add confirmation on successful register/login
--DONE-- - Add logout functionality

struggled a lot with route security because of user_recommendations, had to use
    samesite="None" if IS_PROD else "Lax",
    secure=IS_PROD,

also had to reroute server in vite.config.ts:
  server: {
    proxy: {
      "/api": {
        target: "http://localhost:5000",
        changeOrigin: true,
        secure: false,
      },
    },
  },
and change hardcoded URLs so they don't bypass that if they included credentials.
    fetch("/api/my-list", {
      credentials: "include",
    })

    instead of 

    fetch(`http://127.0.0.1:5000/api/movies/${id}`) {
      credentials: "include",
    }


--DONE FOR NOW-- Improve recommendations (collaborative filtering => account => what user adds to My List)
WORKING ON: User-based recommendations
  --DONE-- Improve the user-based recommendation scripts
      - Recommend movies liked by behaviorally similar users, weighted by similarity strength, while reducing global popularity bias

--DONE-- Make Search results be right clickable/middle clickable so they can be opened in a new tab

--DONE-- Improve the Hero Section, make it scrollable with ~5 movies, dynamic/different on reload
--DONE-- Improve the "Score" rating for a movie (ex: only limit to 2 digits: 7.5, 6.2; color it differently for each range: 1-4 red (bad), 5-7 orange(okay), 8-10 good (green))
--DONE-- Add sorting/
      filtering/
      genres/
--DONE--? better pagination to the main page

--DONE-- Improve feel of responsive design on smaller screens. (genres list vertically, smaller movie cards, smaller text on details page)

--DONE-- Add option to remove movie directly from My List

--DONE-- Make the movie trailer show up when the user clicks "Play" (separate popup window vs full screen?)

--DONE-- Add the separate routes from the navbar (New, etc.)
        --DONE-- Add TV Shows 
          - --DONE-- Fix Trailer API getting /api/movies/549/trailer HTTP/1.1" 200  instead of the TV Show route
          - --DONE-- Add TV Details page
          - --DONE-- Fix DetailsPage not updating instantly on page start if item is in My List/ adding and removing to MyList feels laggy (?minor issue/optimization)
          - --DONE-- ^^ page might be rerendering after a few secs (if load -> add, get confirmation toast of adding -> flickers from +My List to In My List back to +My List)
          - --DONE-- Clicking 'back' on a TV show's details should bring you to the TVPage, not the MoviesPage (MoviesPage was the only/main page before.)
          - --DONE-- Improve My List look (separate Movies and TV Shows?)
          - --DONE-- Add more items to the Hero section rotation (keep only 5 that rotate, but increase variety), increase hero visibility
          - --DONE-- Add 'skeleton' loading to every(?) page
          - --DONE-- Implement recommendations for TV Series (mix/separate?).
          - --DONE-- Fix user recommendations for my list (probably breaks because of TV Shows?) -> had to update DB manually in order to delete older entries (movies that were just saved by id, not by an Object {tmdb_id872585 media_type "movie"})
          - --DONE-- Add TV Show User Recommendations
          - --DONE-- Improve cold start fix (user onboarding on profile creation)
              - --DONE-- Improve look & feel of onboarding page: 
                  - Add a button that asks the user whether they want to start onboarding on the "My List" page (in case they skipped it OR "My List" is empty)
                  - --DONE-- Improve "Pick at least 3 *item* look
                  - --DONE-- Make movie/tv shows container bigger (bigger movie cards on Desktop AND mobile)
                  - --DONE-- TV Shows not appearing on "Action", "Thriller", "Fantasy", "Adventure" genre (for TV Shows they're called "Action & Adventure" "Sci-Fi & Fantasy")
                  - --DONE-- Nothing appearing on the "Sci-Fi" genre (the actual genre is called "Science Fiction")
                  - --DONE-- Add weights to onboarding (seen/like/love)
                  - --DONE-- "Thriller" tag doesn't show any TV Shows (because they don't have the tag)
                    - --DONE-- Fix this by adding more tags to search by (such as Documentary, Talk Show, etc.) that are specific to either Movies or TV Shows and hiding the one that does not fit the tag.
                  - --DONE-- Update the user recommendations to use the weights (love/like)

          - DONE Fix My List look breaking on smaller screens (cards work fine on full res / mobile res, but break in between)
          - DONE Remove unwanted TV series (needs a reseed)

TODO Split My List into "Watched" and "To Watch" 
    - --DONE-- Modified backend route to support this
    - --DONE-- Modified frontend (MovieCard, MyListPage) to add a 'section' prop

TODO Main Movie/TV Show pages seem to double load the Hero? (ex: click on Movies/TV Shows OR refresh page, title + poster appear, new title + poster appear after ~0.5s that replaces that one) (maybe caused by an incorrent useEffect?)

TODO AI Recommendations + Chatbot(?)